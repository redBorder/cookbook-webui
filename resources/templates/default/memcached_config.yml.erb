<% [ "production", "development" ].each do |x| %>
<%= x %>:
  servers:
  <% if @elasticache_hosts.nil? && @elasticache_hosts.empty? %>
    <% @memcached_servers.each do |host| %>
      - "<%= "#{host}.node:11211" %>"
    <% end %>
  <% else %>
    "<%= @elasticache_hosts %>"
  <% end %>
<% end %>
